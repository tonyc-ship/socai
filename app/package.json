{
  "name": "socai-app",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.28.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:desktop": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "build": "tsc && vite build",
    "build:mac": "tauri build --ci --no-sign --bundles app,dmg",
    "build:mac:apple-silicon": "tauri build --ci --no-sign --target aarch64-apple-darwin --bundles app,dmg",
    "build:mac:intel": "tauri build --ci --no-sign --target x86_64-apple-darwin --bundles app,dmg",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
